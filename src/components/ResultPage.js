import React from 'react';

export const ResultPage = ({ teams }) => {
// const sortedTeams = teams.sort((a, b) => a.time - b.time);
    
    return (
      <div>
        <h1>Onneksi olkoon!</h1>
        <p>Tervetuloa takaisin juhliin takapihalle!</p>
      </div>
    );
    };